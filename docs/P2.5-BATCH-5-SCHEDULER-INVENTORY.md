# P2.5 Batch 5 - Legacy Scheduler Inventory

Date: 2026-04-08
Batch task: B5.1
Scope: inventory scheduled/triggered automation paths in `foreman/`.

## Inventory table

| Path | Trigger/cadence | Entry command | Inputs | Outputs | Current owner |
|---|---|---|---|---|---|
| `schedule_agents.sh` | launchd `StartInterval=1800` (30 min) | `run_agent.py chiefofstaff` | `.env`, workspace status files | CoS run log (`logs/chiefofstaff.log`) + workspace updates | launchd timer `ai.jared.chiefofstaff` |
| `loop_agents.py` | continuous loop (`ACTIVE=30s`, `IDLE=300s`) under launchd keepalive | `run_agent.py --all` | agent configs, Supabase task presence, env vars | repeated agent cycles + watchdog lifecycle logs | launchd daemon `ai.jared.agent-loop` |
| `dashboard_sync.py` | dual loops: sync every `60s`, command poll every `10s` | process loop in script | Supabase tables (`dashboard_state`, `commands`), local `agents.json` | dashboard state upserts + command execution results | launchd daemon `ai.jared.dashboard-sync` |
| `batch_runner.py --process` | cron every 15 min (installed by script) | `python batch_runner.py --process` | Anthropic batch IDs from `batch_state.json` | applies completed batch outputs to workspace files + usage logs | crontab entry managed by `batch_runner.py --install-cron` |
| `batch_runner.py --submit` | cron every 3h (installed by script) | `python batch_runner.py --submit` | workspace context snapshots + agent prompt definitions | submitted Anthropic batch jobs + pending state file | crontab entry managed by `batch_runner.py --install-cron` |
| `scripts/launch.sh` orchestration | manual (`launch.sh`, `--status`, `--stop`) | launchd load/unload wrapper | local plist files + health endpoint | starts/stops daemon+timer set (`dashboard`, `tunnel`, `agent-loop`, timers) | operator-driven bootstrap script |
| `run_agent.py` sequential scheduling path | invoked by loop/timers; sequential mode validated in tests | `run_agent.py --all` and per-agent invocations | `users/*/agents.json`, model/provider env, optional Supabase queue checks | agent task execution results, updated task/status artifacts | worker loop + timer callers |

## Scheduling gaps identified

- Scheduling is split across launchd, cron, and in-process loops; there is no single source-of-truth scheduler contract.
- Batch mode and real-time loops can overlap without one centralized conflict/arbitration layer.
- Existing cadence metadata is spread across shell scripts, Python constants, and launchd plist conventions.

## B5.1 completion output

- Legacy scheduler/trigger inventory captured with cadence, entrypoint, input/output surfaces, and owner mapping.
- Next task: B5.2 target architecture for Paperclip-aligned scheduling contract.
