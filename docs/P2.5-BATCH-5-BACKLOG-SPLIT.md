# P2.5 Batch 5 - Automation Backlog Split Plan

Date: 2026-04-08
Batch task: B5.5

## Proposed split

### P2.5a - Core recurring operations migration

- Migrate high-frequency coordinator loops (CoS, dashboard sync adjacency, command polling bridge).
- Convert launchd/cron entrypoints to Paperclip scheduler-compatible wrappers.
- Preserve current reliability guarantees (watchdog, lock semantics, visible failure artifacts).

### P2.5b - Specialist and batch economics migration

- Migrate batch-mode specialist workflows (`batch_runner` submit/process lifecycle).
- Port cost-optimization scheduling with explicit run windows and fallback behavior.
- Retire legacy duplicated scheduling hooks after parity checks pass.

## Prioritization rationale

- P2.5a first reduces operational risk by stabilizing core orchestration.
- P2.5b follows after baseline scheduler contract is proven in production-shaped pilot paths.

## Exit criteria for split acceptance

- Each split has scoped artifacts and named evidence outputs.
- No unresolved overlap between P2.5a and P2.5b responsibilities.
- PM can queue P2.5a immediately after Batch 5 closeout.
