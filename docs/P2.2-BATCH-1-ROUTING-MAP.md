# P2.2 Batch 1 - Routing Inventory and Target Map

Date: 2026-04-08
Batch task: B1.1
Scope: inventory current routing and define target role-to-pod map.

## Current routing inventory

| Layer | Role or path | Current binding | Source of truth |
|---|---|---|---|
| RunPod state | `executor` | Qwen3-14B-AWQ pod endpoint | `state/pods.json` |
| RunPod state | `planner` | Qwen3-30B-A3B-Instruct-2507 pod endpoint | `state/pods.json` |
| RunPod state | `embedding` | Qwen3-Embedding-8B pod endpoint | `state/pods.json` |
| OpenClaw config | default primary model | `executor/Qwen/Qwen3-14B-AWQ` | `~/.openclaw/openclaw.json` |
| OpenClaw providers | `executor`/`planner`/`embedding` providers | All three providers configured with pod base URLs | `~/.openclaw/openclaw.json` and `config/openclaw.foreman.json` |
| Paperclip agent runtime | CEO + OpenClawWorker | `process` adapter -> `scripts/paperclip-openclaw-worker.sh` -> `openclaw agent` | Paperclip API `/companies/<id>/agents` |
| Paperclip role split | planner and embedding role dispatch | Not yet wired as first-class runtime routes | gap identified in B1.1 |

## Current behavior summary

- The infrastructure layer has all three role pods healthy and addressable.
- OpenClaw has provider-per-role definitions, but agent default execution is still executor-first.
- Paperclip currently invokes a single worker command path for both agents, so runtime dispatch is not yet role-specific.
- Planner and embedding are currently validated by `scripts/configure.sh` and `scripts/smoke-test.sh`, not by Paperclip role-based dispatch.

## Target routing map (P2.2 contract)

| Role | Target runtime route | Target pod/provider |
|---|---|---|
| chat and execution | Paperclip task -> OpenClaw runtime (executor route) | `executor` provider (Qwen3-14B-AWQ) |
| planning and reasoning | Paperclip task with planning intent -> OpenClaw runtime (planner route) | `planner` provider (Qwen3-30B-A3B-Instruct-2507) |
| embedding and retrieval | Paperclip embedding path -> OpenClaw runtime (embedding route) | `embedding` provider (Qwen3-Embedding-8B) |

## B1.1 completion output

- Inventory completed: current routing and gaps documented.
- Target role-to-pod map defined for B1.2 implementation.
- Initial source-of-truth contract created at `config/role-routing.json`.
- Next task: B1.2 (enforce this contract in runtime routing paths).
