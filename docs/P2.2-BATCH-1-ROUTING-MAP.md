# P2.2 Batch 1 - Routing Inventory and Target Map

Date: 2026-04-08
Batch task: B1.1
Scope: inventory current routing and define target role-to-provider map.

## Current routing inventory

| Layer | Role or path | Current binding | Source of truth |
|---|---|---|---|
| OpenClaw config | `executor` provider | Executor model endpoint | `~/.openclaw/openclaw.json` |
| OpenClaw config | `planner` provider | Planner model endpoint | `~/.openclaw/openclaw.json` |
| OpenClaw config | `embedding` provider | Embedding model endpoint | `~/.openclaw/openclaw.json` |
| OpenClaw config | default primary model | Executor provider model | `~/.openclaw/openclaw.json` |
| OpenClaw providers | `executor`/`planner`/`embedding` providers | All providers configured from `config/openclaw.foreman.json` | `~/.openclaw/openclaw.json` and `config/openclaw.foreman.json` |
| Paperclip agent runtime | CEO + OpenClawWorker | `process` adapter -> `scripts/paperclip-openclaw-worker.sh` -> `openclaw agent` | Paperclip API `/companies/<id>/agents` |
| Paperclip role split | planner and embedding role dispatch | Not yet wired as first-class runtime routes | gap identified in B1.1 |

## Current behavior summary

- The provider layer has all three role endpoints configured and addressable.
- OpenClaw has provider-per-role definitions, but agent default execution is still executor-first.
- Paperclip currently invokes a single worker command path for both agents, so runtime dispatch is not yet role-specific.
- Planner and embedding are currently validated by `scripts/configure.sh` and role dispatch checks.

## Target routing map (P2.2 contract)

| Role | Target runtime route | Target provider |
|---|---|---|
| chat and execution | Paperclip task -> OpenClaw runtime (executor route) | `executor` provider |
| planning and reasoning | Paperclip task with planning intent -> OpenClaw runtime (planner route) | `planner` provider |
| embedding and retrieval | Paperclip embedding path -> OpenClaw runtime (embedding route) | `embedding` provider |

## B1.1 completion output

- Inventory completed: current routing and gaps documented.
- Target role-to-provider map defined for B1.2 implementation.
- Initial source-of-truth contract created at `config/role-routing.json`.
- Next task: B1.2 (enforce this contract in runtime routing paths).
