# P2.6 Batch 6 - Operator Runbook and Rollback

Date: 2026-04-08
Scope task: B6.5

## Verify tenant isolation

Run:

```bash
./scripts/verify-tenant-isolation-pilot.sh
```

Expected:

- command exits `0`
- output includes `ISOLATION_OK`
- evidence file updated at `state/p2.6-isolation-pilot.json`

## What "green" looks like

- each tenant/company sees its own marker issue
- neither tenant sees the other tenant marker
- evidence file records `a_sees_b=false` and `b_sees_a=false`

## Failure handling

If verification fails:

1. Stop multi-tenant rollout activities for the current batch.
2. Capture evidence and error output in a decision-log entry.
3. Re-run with explicit `PAPERCLIP_COMPANY_A`/`PAPERCLIP_COMPANY_B` overrides to validate reproducibility.
4. Escalate through PM handoff template from P2.4 policy.

## Rollback

- Keep serving only primary tenant/company execution path (`Foreman`) until isolation checks pass.
- Do not promote additional tenant onboarding paths in this batch.
- Record rollback state in `docs/PHASE-1-PLAN.md` and Notion (when MCP sync is available).
